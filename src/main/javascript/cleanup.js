/*global collectionjs */
delete collectionjs.__transporter__;

// Maybe temporary (until the names and inheritance implementations are finalized)
delete collectionjs.protos;
delete collectionjs.prototypes;